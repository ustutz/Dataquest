package;

import python.Lib;

class Script {
	
	static function main() {
		
		var a = Math.sqrt( 16.0 );
		var b = Math.ceil( 111.3 );
		var c = Math.floor( 89.9 );
		
		trace( a );
		trace( b );
		trace( c );
	}

}