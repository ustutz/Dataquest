package;

import python.Lib;

class Script {
	
	static function main() {
		
		var a = Math.sqrt( Math.PI );
		var b = Math.ceil( Math.PI );
		var c = Math.floor( Math.PI );
		
		trace( a );
		trace( b );
		trace( c );
	}

}